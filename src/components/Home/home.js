import ImgSlider from "../imgSlider/imgSlider";
import { Container } from "./home.styles";

const Home = (props) => {
  return (
    <Container>
      <ImgSlider />
    </Container>
  );
};

export default Home;
